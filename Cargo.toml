[workspace]
resolver = "2"
members = ["kernel", "crates/kernel-core", "tools/image"]
default-members = ["crates/kernel-core"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
